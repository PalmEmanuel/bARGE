resources
| where type == "microsoft.storage/storageaccounts" and name contains "bARGE"
| join kind=inner (
    resourcecontainers
    | where type == "microsoft.resources/subscriptions/resourcegroups"
    | project resourceGroup = name, subscriptionId, location
) on subscriptionId
| take 10

